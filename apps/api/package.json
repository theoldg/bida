{
  "name": "@bida/api",
  "version": "0.0.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply hajsik --remote",
    "db:migrate:local": "wrangler d1 migrations apply hajsik --local"
  },
  "dependencies": {
    "@bida/core": "workspace:*",
    "hono": "^4.6.16"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250127.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.0.0"
  }
}
